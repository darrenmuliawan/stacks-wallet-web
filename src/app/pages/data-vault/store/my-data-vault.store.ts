import { atom } from "jotai";

// TODO: change to atomFamily depends on the website
export const personalInfoChecked = atom(false);
export const shoppingAndInterestsChecked = atom(false);
export const locationChecked = atom(false);
export const shouldAutoApproveAllSites = atom(false);
export const settingsVisibility = atom(false);